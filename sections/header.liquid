<link rel="stylesheet" href="{{ 'component-list-menu.css' | asset_url }}" media="print" onload="this.media='all'">
<link rel="stylesheet" href="{{ 'component-search.css' | asset_url }}" media="print" onload="this.media='all'">
<link rel="stylesheet" href="{{ 'component-menu-drawer.css' | asset_url }}" media="print" onload="this.media='all'">
<link
  rel="stylesheet"
  href="{{ 'component-cart-notification.css' | asset_url }}"
  media="print"
  onload="this.media='all'"
>

{%- if settings.predictive_search_enabled -%}
  <link rel="stylesheet" href="{{ 'component-price.css' | asset_url }}" media="print" onload="this.media='all'">
{%- endif -%}

{%- if section.settings.menu_type_desktop == 'mega' -%}
  <link rel="stylesheet" href="{{ 'component-mega-menu.css' | asset_url }}" media="print" onload="this.media='all'">
{%- endif -%}

<style>
  header-drawer {
    justify-self: start;
    margin-left: -1.2rem;
  }

  {%- if section.settings.sticky_header_type == 'reduce-logo-size' -%}
    .scrolled-past-header .header__heading-logo-wrapper {
      width: 75%;
    }
  {%- endif -%}

  {%- if section.settings.menu_type_desktop != "drawer" -%}
    @media screen and (min-width: 990px) {
      header-drawer {
        display: none;
      }
    }
  {%- endif -%}

  .header__mobile-search-trigger {
    display: none;
  }

  @media screen and (max-width: 989px) {
    .header {
      padding: 8px 20px;
      justify-content: space-between;
      align-items: center;
    }

    header-drawer {
      margin-left: 0;
      display: flex;
      align-items: center;
      gap: 12px;
    }

    .header .header__search {
      display: none !important;
    }

    .header__mobile-search-trigger {
      display: flex;
      align-items: center;
      justify-content: center;
      background: none;
      border: none;
      padding: 0;
      cursor: pointer;
      color: inherit;
    }

    /* Hide search icon when menu drawer is open */
    header-drawer .menu-drawer-container[open] ~ .header__mobile-search-trigger {
      display: none;
    }

    /* Hide hamburger icon when drawer is open, show close icon */
    .menu-drawer-container[open] > .header__icon--menu .icon-close {
      display: block;
    }
    .menu-drawer-container:not([open]) > .header__icon--menu .icon-close {
      display: none;
    }
    .menu-drawer-container[open] > .header__icon--menu > span > svg:not(.icon-close) {
      display: none;
    }

    .drawer-accordion__product-card{
      flex: 0 0 auto;
    width: 288px;
    display: flex;
    flex-direction: row;
    align-items: center;
    text-align: center;
    text-decoration: none;
    color: inherit;
    transition: box-shadow 0.2s ease;
    display: flex;
padding: 12px;
justify-content: center;
align-items: center;
border-radius: 20px;
background: rgba(255, 255, 255, 0.50);
gap: 12px;
    }
  }

  .menu-drawer-container {
    display: flex;
  }
  .menu-drawer__menu-item>.svg-wrapper{
    width: 15px;
        position: absolute;
        right: 3rem;
        transform: none !important;
  }

  .list-menu {
    list-style: none;
    padding-top: 8px;
    margin: 0;
    padding-right: 0;
    padding-left: 0;
  }

  .list-menu--inline {
    display: inline-flex;
    flex-wrap: wrap;
  }

  summary.list-menu__item {
    padding-right: 8px;
  }
.header__submenue{
  display: flex;
width: 247px;
padding: 12px 16px;
flex-direction: column;
justify-content: center;
align-items: start;
gap: 12px;
border-radius: 22px;
background: #F2F2F2;
z-index: 10;
    left: 63%;
    top: 32px;
}

/* triangle icon */
.header__submenu-icon{
  position: absolute;
  top: -12px;
  left: 16px;
}
  .list-menu__item {
    display: flex;
    align-items: center;
    line-height: calc(1 + 0.3 / var(--font-body-scale));
    /* margin-bottom: 24px; */
  }
  @media screen and (max-width: 748px) {
    .list-menu__item {
      margin-bottom: 24px;
    }
  }

  .list-menu__item--link {
    text-decoration: none;
    padding-bottom: 1rem;
    padding-top: 1rem;
    line-height: calc(1 + 0.8 / var(--font-body-scale));
  }

  @media screen and (min-width: 750px) {
    .list-menu__item--link {
      padding-bottom: 0.5rem;
      padding-top: 0.5rem;
    }
  }

  .header__custom-dropdown {
    position: absolute;
    width: max-content;
    min-width: 20rem;
    border-radius: 1.2rem;
    padding: 2rem;
    z-index: 10;
    background-color: rgb(var(--color-background));
    box-shadow: 0 4px 24px rgba(0, 0, 0, 0.12);
  }

  .header__custom-block {
    padding: 0;
  }
  .header__custom svg{
    position: absolute;
    top: -7px;
    left: 20px
  }
  .header__custom-dropdown{
    display: flex;
width: 460px;
padding: 12px;
justify-content: center;
align-items: center;
border-radius: 22px;
background: #F2F2F2;

  }
  /* .header__custom-block--product{
position: relative;
display: flex;
width: 460px;
padding: 12px;
justify-content: center;
align-items: center;
border-radius: 22px !important;
background: #F2F2F2;
  } */
.header__custom-block--product svg{
  position: absolute;
    top: -7px;
    left: 24px;
}
  .header__custom-block--custom svg{
    position: absolute;
    top: -20px;
    left: 25px;
  }
  .header__custom-grid {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: 8px;
  }
  .header__custom{
    border-radius: 22px !important;
  background: #F2F2F2;
  padding: 12px !important;
}

  .header__custom-card {
    text-decoration: none;
    display: flex;
max-width: 140px;
height: 106px;
padding: 14px 18px;
flex-direction: column;
justify-content: center;
align-items: center;
gap: 10px;
border-radius: 20px;
background: rgba(255, 255, 255, 0.50);
  }

  .header__custom-card:hover {
    box-shadow: 0 2px 12px rgba(0, 0, 0, 0.1);
    border-color: rgba(var(--color-foreground), 0.15);
  }

  .header__custom-card-image {
    width: 100%;
    display: flex;
    justify-content: center;
    align-items: center;
  }

  .header__custom-card-image img {
    width: 95px;
    height: 55px;
    object-fit: contain;
  }

  .header__custom-card-title {
    color: var(--Black, #1F1F1F);
font-family: "Baloo Bhaijaan 2";
font-size: 14px;
font-weight: 500;
line-height: 120%; /* 16.8px */
letter-spacing: 0px;
text-align: center;
  }

  .header__product-slider-wrapper {
    width: 100%;
  }

  .header__product-slider-heading {
    font-size: 1.4rem;
    font-weight: 600;
    margin: 0 0 1rem;
    color: rgb(var(--color-foreground));
  }

  .header__product-slider {
    display: flex;
    align-items: center;
    gap: 0.8rem;
    position: relative;
  }

  .header__product-slider-track {
    display: flex;
    gap: 8px;
    overflow-x: auto;
    scroll-behavior: smooth;
    scrollbar-width: none;
    -ms-overflow-style: none;
    flex: 1;
    /* padding: 0.4rem 0; */
  }

  .header__product-slider-track::-webkit-scrollbar {
    display: none;
  }

  .header__product-slide {
    display: flex;
padding: 12px 8px;
flex-direction: column;
/* justify-content: center; */
align-items: center;
/* flex: 1 0 0; */
min-width: 140px;
min-height: 186px;
border-radius: 20px;
background: rgba(255, 255, 255, 0.50);
  }

  .header__product-slide a {
    text-decoration: none;
    color: inherit;
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: 8px;
    /* padding: 1rem; */
    /* border: 1px solid rgba(var(--color-foreground), 0.08); */
    border-radius: 1rem;
    text-align: center;
    transition: box-shadow 0.2s ease, border-color 0.2s ease;
  }

  .header__product-slide a:hover {
    box-shadow: 0 2px 12px rgba(0, 0, 0, 0.1);
    border-color: rgba(var(--color-foreground), 0.15);
  }

  .header__product-slide img {
    width: 70px;
height: 70px;
flex-shrink: 0;
aspect-ratio: 1/1;
  }
  .header__product-slide-image{
    display: flex;
width: 87.5px;
height: 87.5px;
padding: 9.055px 8.736px 8.445px 8.764px;
justify-content: center;
align-items: center;
flex-shrink: 0;
    border-radius: 16.875px;
    background: #F8F8F6;
  }
  /* .header__product-slide span {
    font-size: 1.2rem;
    font-weight: 500;
    line-height: 1.3;
  } */

  .header__product-slide-price {
    color: var(--Black, #1F1F1F);
font-size: 14px;
font-weight: 500;
letter-spacing: 0px;
line-height: 8px;
    margin-top: 4px;
  }
.header__product-slide-title{
  color: var(--Black, #1F1F1F);
font-size: 16px;
font-weight: 500;
line-height: 10px; /* 19.2px */
letter-spacing: 0px;
}

  .header__product-description {
    display: block;
    color: rgba(31, 31, 31, 0.80);
text-align: center;
font-family: "Baloo Bhaijaan 2";
font-size: 14px;
font-weight: 400;
line-height: 120%; /* 16.8px */
letter-spacing: 0px;
  }

  .header__product-slider-btn {
    display: flex;
width: 32px;
height: 32px;
padding: 5.333px 9.143px 5.333px 7.619px;
justify-content: center;
align-items: center;
gap: 7.619px;
flex-shrink: 0;
border-radius: 75.429px;
border: 0.762px solid rgba(0, 54, 101, 0.23);
background: var(--White, #FFF);
    transition: background 0.2s ease;
    z-index: 2;
  }

  .header__product-slider-btn:hover {
    background: rgba(var(--color-foreground), 0.05);
  }

  .header__product-slider-btn svg {
    width: 1rem;
    height: 1rem;
  }

  .header__product-slider-btn--prev svg {
    transform: rotate(90deg);
  }
  .header__product-slider-btn--prev{
    position: absolute;
    left: -7.62px;
  }
  .header__product-slider-btn--next{
    position: absolute;
    right: -7.62px;
  }

  .header__product-slider-btn--next svg {
    transform: rotate(-90deg);
  }

  .header__custom-placeholder {
    padding: 0.4rem 0;
  }

  .drawer-accordion {
    list-style: none;
  }

  .drawer-accordion__child-list {
    list-style: none;
    padding: 0;
    margin: 0;
  }

  .drawer-accordion__custom-item {
    list-style: none;
  }

.drawer-accordion__toggle, .menu-drawer__navigation > ul > li > .menu-drawer__menu-item{
    width: 100%;
    background: none;
    border: none;
    cursor: pointer;
    display: flex;
    align-items: center;
    justify-content: space-between;
    font: inherit;
    color: #000;
    font-size: 18px !important;
    font-style: normal;
    font-weight: 500;
    line-height: 11px;
    margin: 24px 0;
    letter-spacing: 0;
  }

  .drawer-accordion__icon svg {
    display: block;
    transition: transform 0.3s ease;
    transform-origin: center center;
  }

  .drawer-accordion__toggle[aria-expanded="true"] .drawer-accordion__icon svg {
    transform: rotate(180deg);
  }

  .drawer-accordion__content {
    max-height: 0;
    overflow: hidden;
    transition: max-height 0.4s ease;
  }

  .drawer-accordion__content.is-open {
    max-height: 200rem;
  }
  

  /* .drawer-accordion__grid {
    display: grid;
    grid-template-columns: repeat(2, 1fr);
    gap: 1rem;
    padding: 1.2rem 0.4rem;
  } */

  @media screen and (max-width: 749px) {
    .drawer-accordion__grid {
      display: flex;
      flex-direction: row;
      flex-wrap: nowrap;
      gap: 12px;
      overflow-x: auto;
      overflow-y: hidden;
      scroll-behavior: smooth;
      -webkit-overflow-scrolling: touch;
      scrollbar-width: none;
      -ms-overflow-style: none;
    }

    .drawer-accordion__grid::-webkit-scrollbar {
      display: none;
    }

    .drawer-accordion__grid .drawer-accordion__card {
      display: flex;
width: 140px;
height: 106px;
padding: 14px 18px;
flex-direction: column;
justify-content: center;
align-items: center;
gap: 10px;
border-radius: 20px;
background: rgba(255, 255, 255, 0.50);
margin-bottom: 24px;
    }
  }

  .drawer-accordion__card {
    display: flex;
    flex-direction: column;
    align-items: center;
    text-align: center;
    text-decoration: none;
    color: inherit;
    border: 1px solid rgba(var(--color-foreground), 0.08);
    border-radius: 0.8rem;
    padding: 1rem 0.6rem;
    gap: 0.6rem;
    transition: box-shadow 0.2s ease;
  }

  .drawer-accordion__card:hover {
    box-shadow: 0 2px 8px rgba(0, 0, 0, 0.08);
  }

  .drawer-accordion__card img {
    width: 70px;
    height: 70px;
    object-fit: contain;
  }

  .drawer-accordion__card span {
    color: #1F1F1F;
    font-size: 14px;
    font-style: normal;
    font-weight: 500;
    line-height: 120%;
    letter-spacing: 0;
  }

  .drawer-accordion__slider {
    padding: 1.2rem 0.4rem;
  }

  .drawer-accordion__slider-heading {
    font-size: 1.3rem;
    font-weight: 600;
    margin: 0 0 0.8rem;
  }

  .drawer-accordion__slider-track {
    display: flex;
    gap: 1rem;
    overflow-x: auto;
    scroll-behavior: smooth;
    scrollbar-width: none;
    -ms-overflow-style: none;
    padding-bottom: 0;
  }
  .js .menu-drawer__menu-item.drawer-accordion__toggle .icon-caret path {
      color: #808080;
      fill: #808080;
  }
  .js .menu-drawer__menu-item.drawer-accordion__toggle .icon-caret {
      width: 10px;
  }

  .drawer-accordion__slider-track::-webkit-scrollbar {
    display: none;
  }

  .drawer-accordion__slider-scrollbar {
    margin:24px 0;
    user-select: none;
  }

  .drawer-accordion__slider-scrollbar-track {
    position: relative;
    height: 3px;
    background: rgba(204, 204, 204, 0.4);
    border-radius: 16px;
    max-width: 390px;
  }

  .drawer-accordion__slider-scrollbar-thumb {
    position: absolute;
    top: 0;
    left: 0;
    height: 100%;
    min-width: 2.4rem;
    background: #003665;
    border-radius: 16px;
    cursor: grab;
    display: block !important;
    touch-action: none;
  }

  .drawer-accordion__slider-scrollbar-thumb:active {
    cursor: grabbing;
  }


  /* .drawer-accordion__product-card { */
    /* flex: 0 0 auto;
    width: 12rem;
    display: flex;
    flex-direction: column;
    align-items: center;
    text-align: center;
    text-decoration: none;
    color: inherit;
    border: 1px solid rgba(var(--color-foreground), 0.08);
    border-radius: 0.8rem;
    padding: 0.8rem;
    gap: 0.4rem;
    transition: box-shadow 0.2s ease;
  } */

  .drawer-accordion__product-card:hover {
    box-shadow: 0 2px 8px rgba(0, 0, 0, 0.08);
  }

  .drawer-accordion__product-card img {
    width: 70px;
    height: 70px;
    object-fit: contain;
  }

  .drawer-accordion__product-card span {
    color: var(--Black, #1F1F1F);
    font-size: 16px;
    font-weight: 500;
    line-height: 120%; /* 19.2px */
    letter-spacing: 0;
  }

    .drawer-accordion__product-card span.drawer-accordion__product-price {
    color: #1F1F1F;
    font-size: 14px;
    font-weight: 500;
    line-height: 8px;
    letter-spacing: 0px;
  }
  .drawer-accordion__product-card-content{
    display: flex;
    flex-direction: column;
    gap: 8px;
  }
.drawer-accordion__product-card-title {
  color: var(--Black, #1F1F1F);
leading-trim: both;
text-edge: cap;
font-family: "Baloo Bhaijaan 2";
font-size: 16px;
font-style: normal;
font-weight: 500;
line-height: 120%; /* 19.2px */}

.drawer-accordion__product-card span.drawer-accordion__product-description {
    font-size: 14px;
    font-weight: 400;
    line-height: 1.35;
    letter-spacing: 0;
    color: rgba(31, 31, 31, 0.80);
    letter-spacing: 0;
    opacity: 0.6;
}
.header__search-bar-inner {
  border-radius: 18px;
  height: 24px;
  background: #FFF;
  border: none;
  padding: 6px 8px;
}
#menu-drawer .drawer-accordion__content.is-open {
  overflow: visible;
}
#menu-drawer .drawer-accordion__grid {
  max-width: 100vw;
  margin: 0 -20px;
  padding: 0 20px;
}
  .header:not(.drawer-menu).page-width .header__icons .header__search.is-expanded .header__search-bar {
      max-width: 250px;
  }

  @media screen and (max-width: 989.98px) {
    .drawer-accordion__product-card-content {
        text-align: left;
    }
  }
</style>

{%- style -%}
  .header {
    padding: 8px 20px;
  }

  .section-header {
    position: sticky; /* This is for fixing a Safari z-index issue. PR #2147 */
    margin-bottom: {{ section.settings.margin_bottom | times: 0.75 | round: 0 }}px;
  }

  @media screen and (min-width: 750px) {
    .section-header {
      margin-bottom: {{ section.settings.margin_bottom }}px;
    }
  }

  @media screen and (min-width: 990px) {
    .header {
      padding-top: {{ section.settings.padding_top }}px;
      padding-bottom: {{ section.settings.padding_bottom }}px;
    }
  }
{%- endstyle -%}

<script src="{{ 'cart-notification.js' | asset_url }}" defer="defer"></script>

{%- liquid
  for block in section.blocks
    if block.type == '@app'
      assign has_app_block = true
    endif
  endfor
-%}

{% liquid
  assign header_tag = 'div'

  if section.settings.sticky_header_type != 'none'
    assign header_tag = 'sticky-header'
  endif
%}

<{{ header_tag }}
  {% if header_tag == 'sticky-header' %}
    data-sticky-type="{{ section.settings.sticky_header_type }}"
  {% endif %}
  class="header-wrapper color-{{ section.settings.color_scheme }} gradient{% if section.settings.show_line_separator %} header-wrapper--border-bottom{% endif %}"
>
  {%- liquid
    assign social_links = false
    assign localization_forms = false

    if settings.social_twitter_link != blank or settings.social_facebook_link != blank or settings.social_pinterest_link != blank or settings.social_instagram_link != blank or settings.social_youtube_link != blank or settings.social_vimeo_link != blank or settings.social_tiktok_link != blank or settings.social_tumblr_link != blank or settings.social_snapchat_link != blank
      assign social_links = true
    endif

    if localization.available_countries.size > 1 and section.settings.enable_country_selector or section.settings.enable_language_selector and localization.available_languages.size > 1
      assign localization_forms = true
    endif
  -%}
  <header class="header header--{{ section.settings.logo_position }} header--mobile-{{ section.settings.mobile_logo_position }} page-width{% if section.settings.menu_type_desktop == 'drawer' %} drawer-menu{% endif %}{% if section.settings.menu != blank %} header--has-menu{% endif %}{% if has_app_block %} header--has-app{% endif %}{% if social_links %} header--has-social{% endif %}{% if shop.customer_accounts_enabled %} header--has-account{% endif %}{% if localization_forms %} header--has-localizations{% endif %}">
    {%- liquid
      if section.settings.menu != blank
        render 'header-drawer'
      endif

      if section.settings.logo_position == 'top-center' or section.settings.menu == blank
        render 'header-search', input_id: 'Search-In-Modal-1'
      endif
    -%}

    {%- if section.settings.logo_position != 'middle-center' -%}
      {%- if request.page_type == 'index' -%}
        <h1 class="header__heading">
      {%- endif -%}
      <a href="{{ routes.root_url }}" class="header__heading-link link link--text focus-inset">
        {%- if settings.logo != blank -%}
          <div class="header__heading-logo-wrapper">
            {%- assign logo_alt = settings.logo.alt | default: shop.name | escape -%}
            {%- assign logo_height = settings.logo_width | divided_by: settings.logo.aspect_ratio -%}
            {% capture sizes %}(max-width: {{ settings.logo_width | times: 2 }}px) 50vw, {{ settings.logo_width }}px{% endcapture %}
            {% capture widths %}{{ settings.logo_width }}, {{ settings.logo_width | times: 1.5 | round }}, {{ settings.logo_width | times: 2 }}{% endcapture %}
            {{
              settings.logo
              | image_url: width: 600
              | image_tag:
                class: 'header__heading-logo motion-reduce',
                widths: widths,
                height: logo_height,
                width: settings.logo_width,
                alt: logo_alt,
                sizes: sizes,
                preload: true
            }}
          </div>
        {%- else -%}
          <span class="h2">{{ shop.name }}</span>
        {%- endif -%}
      </a>
      {%- if request.page_type == 'index' -%}
        </h1>
      {%- endif -%}
    {%- endif -%}

    {%- liquid
      if section.settings.menu != blank
        if section.settings.menu_type_desktop == 'dropdown'
          render 'header-dropdown-menu'
        elsif section.settings.menu_type_desktop != 'drawer'
          render 'header-mega-menu'
        endif
      endif
    %}

    {%- if section.settings.logo_position == 'middle-center' -%}
      {%- if request.page_type == 'index' -%}
        <h1 class="header__heading">
      {%- endif -%}
      <a href="{{ routes.root_url }}" class="header__heading-link link link--text focus-inset">
        {%- if settings.logo != blank -%}
          <div class="header__heading-logo-wrapper">
            {%- assign logo_alt = settings.logo.alt | default: shop.name | escape -%}
            {%- assign logo_height = settings.logo_width | divided_by: settings.logo.aspect_ratio -%}
            {% capture sizes %}(min-width: 750px) {{ settings.logo_width }}px, 50vw{% endcapture %}
            {% capture widths %}{{ settings.logo_width }}, {{ settings.logo_width | times: 1.5 | round }}, {{ settings.logo_width | times: 2 }}{% endcapture %}
            {{
              settings.logo
              | image_url: width: 600
              | image_tag:
                class: 'header__heading-logo',
                widths: widths,
                height: logo_height,
                width: settings.logo_width,
                alt: logo_alt,
                sizes: sizes,
                preload: true
            }}
          </div>
        {%- else -%}
          <span class="h2">{{ shop.name }}</span>
        {%- endif -%}
      </a>
      {%- if request.page_type == 'index' -%}
        </h1>
      {%- endif -%}
    {%- endif -%}

    <div class="header__icons{% if section.settings.enable_country_selector or section.settings.enable_language_selector %} header__icons--localization header-localization{% endif %}">
      <div class="desktop-localization-wrapper">
        {%- if section.settings.enable_country_selector and localization.available_countries.size > 1 -%}
          <localization-form class="small-hide medium-hide" data-prevent-hide>
            {%- form 'localization', id: 'HeaderCountryForm', class: 'localization-form' -%}
              <div>
                <h2 class="visually-hidden" id="HeaderCountryLabel">{{ 'localization.country_label' | t }}</h2>
                {%- render 'country-localization', localPosition: 'HeaderCountry' -%}
              </div>
            {%- endform -%}
          </localization-form>
        {% endif %}

        {%- if section.settings.enable_language_selector and localization.available_languages.size > 1 -%}
          <localization-form class="small-hide medium-hide" data-prevent-hide>
            {%- form 'localization', id: 'HeaderLanguageForm', class: 'localization-form' -%}
              <div>
                <h2 class="visually-hidden" id="HeaderLanguageLabel">{{ 'localization.language_label' | t }}</h2>
                {%- render 'language-localization', localPosition: 'HeaderLanguage' -%}
              </div>
            {%- endform -%}
          </localization-form>
        {%- endif -%}
      </div>
      {% render 'header-search', input_id: 'Search-In-Modal' %}

      {% comment %} {%- if shop.customer_accounts_enabled -%}
        <a
          href="{%- if customer -%}{{ routes.account_url }}{%- else -%}{{ routes.account_login_url }}{%- endif -%}"
          class="header__icon header__icon--account link focus-inset{% if section.settings.menu != blank %} small-hide{% endif %}"
          rel="nofollow"
        >
          {%- if section.settings.enable_customer_avatar -%}
            <account-icon>
              {%- if customer and customer.has_avatar? -%}
                {{ customer | avatar }}
              {%- else -%}
                <span class="svg-wrapper">{{ 'icon-account.svg' | inline_asset_content }}</span>
              {%- endif -%}
            </account-icon>
          {%- else -%}
            <span class="svg-wrapper">{{ 'icon-account.svg' | inline_asset_content }}</span>
          {%- endif -%}
          <span class="visually-hidden">
            {%- liquid
              if customer
                echo 'customer.account_fallback' | t
              else
                echo 'customer.log_in' | t
              endif
            -%}
          </span>
        </a>
      {%- endif -%} {% endcomment %}

      {%- for block in section.blocks -%}
        {%- case block.type -%}
          {%- when '@app' -%}
            {% render block %}
        {%- endcase -%}
      {%- endfor -%}

      <a href="{{ routes.cart_url }}" class="header__icon header__icon--cart link focus-inset" id="cart-icon-bubble">
        {% if cart == empty %}
          <span class="svg-wrapper"><svg xmlns="http://www.w3.org/2000/svg" width="19" height="19" viewBox="0 0 19 19" fill="none">
          <path d="M15 15C15.5304 15 16.0391 15.2107 16.4142 15.5858C16.7893 15.9609 17 16.4696 17 17C17 17.5304 16.7893 18.0391 16.4142 18.4142C16.0391 18.7893 15.5304 19 15 19C14.4696 19 13.9609 18.7893 13.5858 18.4142C13.2107 18.0391 13 17.5304 13 17C13 16.4696 13.2107 15.9609 13.5858 15.5858C13.9609 15.2107 14.4696 15 15 15ZM15 16C14.7348 16 14.4804 16.1054 14.2929 16.2929C14.1054 16.4804 14 16.7348 14 17C14 17.2652 14.1054 17.5196 14.2929 17.7071C14.4804 17.8946 14.7348 18 15 18C15.2652 18 15.5196 17.8946 15.7071 17.7071C15.8946 17.5196 16 17.2652 16 17C16 16.7348 15.8946 16.4804 15.7071 16.2929C15.5196 16.1054 15.2652 16 15 16ZM6 15C6.53043 15 7.03914 15.2107 7.41421 15.5858C7.78929 15.9609 8 16.4696 8 17C8 17.5304 7.78929 18.0391 7.41421 18.4142C7.03914 18.7893 6.53043 19 6 19C5.46957 19 4.96086 18.7893 4.58579 18.4142C4.21071 18.0391 4 17.5304 4 17C4 16.4696 4.21071 15.9609 4.58579 15.5858C4.96086 15.2107 5.46957 15 6 15ZM6 16C5.73478 16 5.48043 16.1054 5.29289 16.2929C5.10536 16.4804 5 16.7348 5 17C5 17.2652 5.10536 17.5196 5.29289 17.7071C5.48043 17.8946 5.73478 18 6 18C6.26522 18 6.51957 17.8946 6.70711 17.7071C6.89464 17.5196 7 17.2652 7 17C7 16.7348 6.89464 16.4804 6.70711 16.2929C6.51957 16.1054 6.26522 16 6 16ZM17 3H3.27L5.82 9H14C14.33 9 14.62 8.84 14.8 8.6L17.8 4.6C17.93 4.43 18 4.22 18 4C18 3.73478 17.8946 3.48043 17.7071 3.29289C17.5196 3.10536 17.2652 3 17 3ZM14 10H5.87L5.1 11.56L5 12C5 12.2652 5.10536 12.5196 5.29289 12.7071C5.48043 12.8946 5.73478 13 6 13H17V14H6C5.46957 14 4.96086 13.7893 4.58579 13.4142C4.21071 13.0391 4 12.5304 4 12C3.9997 11.6607 4.08573 11.3269 4.25 11.03L4.97 9.56L1.34 1H0V0H2L2.85 2H17C17.5304 2 18.0391 2.21071 18.4142 2.58579C18.7893 2.96086 19 3.46957 19 4C19 4.5 18.83 4.92 18.55 5.26L15.64 9.15C15.28 9.66 14.68 10 14 10Z" fill="#1F1F1F"/>
        </svg></span>
        {% else %}
          <span class="svg-wrapper"><svg xmlns="http://www.w3.org/2000/svg" width="19" height="19" viewBox="0 0 19 19" fill="none">
          <path d="M15 15C15.5304 15 16.0391 15.2107 16.4142 15.5858C16.7893 15.9609 17 16.4696 17 17C17 17.5304 16.7893 18.0391 16.4142 18.4142C16.0391 18.7893 15.5304 19 15 19C14.4696 19 13.9609 18.7893 13.5858 18.4142C13.2107 18.0391 13 17.5304 13 17C13 16.4696 13.2107 15.9609 13.5858 15.5858C13.9609 15.2107 14.4696 15 15 15ZM15 16C14.7348 16 14.4804 16.1054 14.2929 16.2929C14.1054 16.4804 14 16.7348 14 17C14 17.2652 14.1054 17.5196 14.2929 17.7071C14.4804 17.8946 14.7348 18 15 18C15.2652 18 15.5196 17.8946 15.7071 17.7071C15.8946 17.5196 16 17.2652 16 17C16 16.7348 15.8946 16.4804 15.7071 16.2929C15.5196 16.1054 15.2652 16 15 16ZM6 15C6.53043 15 7.03914 15.2107 7.41421 15.5858C7.78929 15.9609 8 16.4696 8 17C8 17.5304 7.78929 18.0391 7.41421 18.4142C7.03914 18.7893 6.53043 19 6 19C5.46957 19 4.96086 18.7893 4.58579 18.4142C4.21071 18.0391 4 17.5304 4 17C4 16.4696 4.21071 15.9609 4.58579 15.5858C4.96086 15.2107 5.46957 15 6 15ZM6 16C5.73478 16 5.48043 16.1054 5.29289 16.2929C5.10536 16.4804 5 16.7348 5 17C5 17.2652 5.10536 17.5196 5.29289 17.7071C5.48043 17.8946 5.73478 18 6 18C6.26522 18 6.51957 17.8946 6.70711 17.7071C6.89464 17.5196 7 17.2652 7 17C7 16.7348 6.89464 16.4804 6.70711 16.2929C6.51957 16.1054 6.26522 16 6 16ZM17 3H3.27L5.82 9H14C14.33 9 14.62 8.84 14.8 8.6L17.8 4.6C17.93 4.43 18 4.22 18 4C18 3.73478 17.8946 3.48043 17.7071 3.29289C17.5196 3.10536 17.2652 3 17 3ZM14 10H5.87L5.1 11.56L5 12C5 12.2652 5.10536 12.5196 5.29289 12.7071C5.48043 12.8946 5.73478 13 6 13H17V14H6C5.46957 14 4.96086 13.7893 4.58579 13.4142C4.21071 13.0391 4 12.5304 4 12C3.9997 11.6607 4.08573 11.3269 4.25 11.03L4.97 9.56L1.34 1H0V0H2L2.85 2H17C17.5304 2 18.0391 2.21071 18.4142 2.58579C18.7893 2.96086 19 3.46957 19 4C19 4.5 18.83 4.92 18.55 5.26L15.64 9.15C15.28 9.66 14.68 10 14 10Z" fill="#1F1F1F"/>
        </svg></span>
        {% endif %}
        <span class="visually-hidden">{{ 'templates.cart.cart' | t }}</span>
        {%- if cart != empty -%}
          <div class="cart-count-bubble">
            {%- if cart.item_count < 100 -%}
              <span aria-hidden="true">{{ cart.item_count }}</span>
            {%- endif -%}
            <span class="visually-hidden">{{ 'sections.header.cart_count' | t: count: cart.item_count }}</span>
          </div>
        {%- endif -%}
      </a>
    </div>
  </header>
</{{ header_tag }}>

{%- if settings.cart_type == 'notification' -%}
  {%- render 'cart-notification',
    color_scheme: section.settings.color_scheme,
    desktop_menu_type: section.settings.menu_type_desktop
  -%}
{%- endif -%}

{% javascript %}
  class StickyHeader extends HTMLElement {
    constructor() {
      super();
    }

    connectedCallback() {
      this.header = document.querySelector('.section-header');
      this.headerIsAlwaysSticky =
        this.getAttribute('data-sticky-type') === 'always' ||
        this.getAttribute('data-sticky-type') === 'reduce-logo-size';
      this.headerBounds = {};

      this.setHeaderHeight();

      window.matchMedia('(max-width: 990px)').addEventListener('change', this.setHeaderHeight.bind(this));

      if (this.headerIsAlwaysSticky) {
        this.header.classList.add('shopify-section-header-sticky');
      }

      this.currentScrollTop = 0;
      this.preventReveal = false;
      this.predictiveSearch = this.querySelector('predictive-search');

      this.onScrollHandler = this.onScroll.bind(this);
      this.hideHeaderOnScrollUp = () => (this.preventReveal = true);

      this.addEventListener('preventHeaderReveal', this.hideHeaderOnScrollUp);
      window.addEventListener('scroll', this.onScrollHandler, false);

      this.createObserver();
    }

    setHeaderHeight() {
      document.documentElement.style.setProperty('--header-height', `${this.header.offsetHeight}px`);
    }

    disconnectedCallback() {
      this.removeEventListener('preventHeaderReveal', this.hideHeaderOnScrollUp);
      window.removeEventListener('scroll', this.onScrollHandler);
    }

    createObserver() {
      let observer = new IntersectionObserver((entries, observer) => {
        this.headerBounds = entries[0].intersectionRect;
        observer.disconnect();
      });

      observer.observe(this.header);
    }

    onScroll() {
      const scrollTop = window.pageYOffset || document.documentElement.scrollTop;

      if (this.predictiveSearch && this.predictiveSearch.isOpen) return;

      if (scrollTop > this.currentScrollTop && scrollTop > this.headerBounds.bottom) {
        this.header.classList.add('scrolled-past-header');
        if (this.preventHide) return;
        requestAnimationFrame(this.hide.bind(this));
      } else if (scrollTop < this.currentScrollTop && scrollTop > this.headerBounds.bottom) {
        this.header.classList.add('scrolled-past-header');
        if (!this.preventReveal) {
          requestAnimationFrame(this.reveal.bind(this));
        } else {
          window.clearTimeout(this.isScrolling);

          this.isScrolling = setTimeout(() => {
            this.preventReveal = false;
          }, 66);

          requestAnimationFrame(this.hide.bind(this));
        }
      } else if (scrollTop <= this.headerBounds.top) {
        this.header.classList.remove('scrolled-past-header');
        requestAnimationFrame(this.reset.bind(this));
      }

      this.currentScrollTop = scrollTop;
    }

    hide() {
      if (this.headerIsAlwaysSticky) return;
      this.header.classList.add('shopify-section-header-hidden', 'shopify-section-header-sticky');
      this.closeMenuDisclosure();
      this.closeSearchModal();
    }

    reveal() {
      if (this.headerIsAlwaysSticky) return;
      this.header.classList.add('shopify-section-header-sticky', 'animate');
      this.header.classList.remove('shopify-section-header-hidden');
    }

    reset() {
      if (this.headerIsAlwaysSticky) return;
      this.header.classList.remove('shopify-section-header-hidden', 'shopify-section-header-sticky', 'animate');
    }

    closeMenuDisclosure() {
      this.disclosures = this.disclosures || this.header.querySelectorAll('header-menu');
      this.disclosures.forEach((disclosure) => disclosure.close());
    }

    closeSearchModal() {
      var searchContainer = this.header.querySelector('[data-header-search]');
      if (searchContainer) {
        searchContainer.classList.remove('is-expanded');
        var input = searchContainer.querySelector('.header__search-bar-input');
        if (input) input.value = '';
      }
    }
  }

  customElements.define('sticky-header', StickyHeader);

  document.addEventListener('DOMContentLoaded', function () {
    document.querySelectorAll('.header__product-slider').forEach(function (slider) {
      const track = slider.querySelector('.header__product-slider-track');
      const prevBtn = slider.querySelector('.header__product-slider-btn--prev');
      const nextBtn = slider.querySelector('.header__product-slider-btn--next');
      const scrollAmount = 280;

      if (prevBtn && track) {
        prevBtn.addEventListener('click', function () {
          track.scrollBy({ left: -scrollAmount, behavior: 'smooth' });
        });
      }
      if (nextBtn && track) {
        nextBtn.addEventListener('click', function () {
          track.scrollBy({ left: scrollAmount, behavior: 'smooth' });
        });
      }
    });

    document.querySelectorAll('.drawer-accordion__toggle').forEach(function (btn) {
      btn.addEventListener('click', function (e) {
        e.stopPropagation();
        e.preventDefault();
        const expanded = btn.getAttribute('aria-expanded') === 'true';
        const content = btn.nextElementSibling;
        btn.setAttribute('aria-expanded', String(!expanded));
        content.classList.toggle('is-open', !expanded);
      });
    });

    document.querySelectorAll('.drawer-accordion__slider').forEach(function (slider) {
      const track = slider.querySelector('.drawer-accordion__slider-track');
      const scrollbar = slider.querySelector('.drawer-accordion__slider-scrollbar');
      const scrollbarTrack = slider.querySelector('.drawer-accordion__slider-scrollbar-track');
      const thumb = slider.querySelector('.drawer-accordion__slider-scrollbar-thumb');
      if (!track || !scrollbar || !scrollbarTrack || !thumb) return;

      function updateCustomScrollbar() {
        var scrollWidth = track.scrollWidth;
        var clientWidth = track.clientWidth;
        var maxScroll = scrollWidth - clientWidth;
        if (maxScroll <= 0) {
          scrollbar.style.display = 'none';
          return;
        }
        scrollbar.style.display = '';
        var barWidth = scrollbarTrack.clientWidth;
        var thumbWidth = Math.max((clientWidth / scrollWidth) * barWidth, 24);
        var maxThumbLeft = Math.max(0, barWidth - thumbWidth);
        var ratio = maxScroll > 0 ? track.scrollLeft / maxScroll : 0;
        thumb.style.width = thumbWidth + 'px';
        thumb.style.left = ratio * maxThumbLeft + 'px';
      }

      track.addEventListener('scroll', updateCustomScrollbar);
      window.addEventListener('resize', updateCustomScrollbar);
      var ro = new ResizeObserver(updateCustomScrollbar);
      ro.observe(track);
      ro.observe(scrollbarTrack);

      track.querySelectorAll('img').forEach(function (img) {
        img.addEventListener('load', updateCustomScrollbar);
      });

      thumb.addEventListener('pointerdown', function (e) {
        if (e.button !== 0) return;
        e.preventDefault();
        var startX = e.clientX;
        var startScrollLeft = track.scrollLeft;
        var maxScroll = track.scrollWidth - track.clientWidth;
        var barWidth = scrollbarTrack.clientWidth;
        var thumbWidth = thumb.offsetWidth;
        var maxThumbLeft = Math.max(0, barWidth - thumbWidth);

        function onMove(ev) {
          var dx = ev.clientX - startX;
          var scrollDelta = maxThumbLeft > 0 ? (dx / maxThumbLeft) * maxScroll : 0;
          track.scrollLeft = Math.max(0, Math.min(maxScroll, startScrollLeft + scrollDelta));
        }
        function onUp() {
          document.removeEventListener('pointermove', onMove);
          document.removeEventListener('pointerup', onUp);
        }
        document.addEventListener('pointermove', onMove);
        document.addEventListener('pointerup', onUp);
      });

      scrollbarTrack.addEventListener('pointerdown', function (e) {
        if (e.target === thumb) return;
        var rect = scrollbarTrack.getBoundingClientRect();
        var x = e.clientX - rect.left;
        var barWidth = scrollbarTrack.clientWidth;
        var scrollWidth = track.scrollWidth;
        var clientWidth = track.clientWidth;
        var thumbWidth = Math.max((clientWidth / scrollWidth) * barWidth, 24);
        var maxScroll = scrollWidth - clientWidth;
        var maxThumbLeft = Math.max(0, barWidth - thumbWidth);
        var targetLeft = Math.max(0, Math.min(maxThumbLeft, x - thumbWidth / 2));
        var ratio = maxThumbLeft > 0 ? targetLeft / maxThumbLeft : 0;
        track.scrollLeft = ratio * maxScroll;
      });

      updateCustomScrollbar();
    });
  });

  document.querySelectorAll('.header__mobile-search-trigger').forEach(function(btn) {
    btn.addEventListener('click', function() {
      var headerDrawer = btn.closest('header-drawer');
      if (!headerDrawer) return;
      var summary = headerDrawer.querySelector('summary');
      if (!summary) return;
      var details = summary.parentElement;
      if (details && !details.open) {
        summary.click();
        setTimeout(function() {
          var searchInput = headerDrawer.querySelector('.menu-drawer__search input[type="search"]');
          if (searchInput) searchInput.focus();
        }, 350);
      }
    });
  });

  document.querySelectorAll('[data-header-search]').forEach(function(searchContainer) {
    var toggleBtn = searchContainer.querySelector('[data-search-toggle]');
    var closeBtn = searchContainer.querySelector('[data-search-close]');
    var searchInput = searchContainer.querySelector('.header__search-bar-input');

    if (toggleBtn) {
      toggleBtn.addEventListener('click', function(e) {
        e.preventDefault();
        searchContainer.classList.add('is-expanded');
        if (searchInput) {
          setTimeout(function() { searchInput.focus(); }, 100);
        }
      });
    }

    if (closeBtn) {
      closeBtn.addEventListener('click', function(e) {
        e.preventDefault();
        searchContainer.classList.remove('is-expanded');
        if (searchInput) searchInput.value = '';
      });
    }

    document.addEventListener('click', function(e) {
      if (!searchContainer.contains(e.target) && searchContainer.classList.contains('is-expanded')) {
        searchContainer.classList.remove('is-expanded');
        if (searchInput) searchInput.value = '';
      }
    });

    if (searchInput) {
      searchInput.addEventListener('keyup', function(e) {
        if (e.key === 'Escape') {
          searchContainer.classList.remove('is-expanded');
          searchInput.value = '';
          if (toggleBtn) toggleBtn.focus();
        }
      });
    }
  });
{% endjavascript %}

<script type="application/ld+json">
  {
    "@context": "http://schema.org",
    "@type": "Organization",
    "name": {{ shop.name | json }},
    {% if settings.logo %}
      "logo": {{ settings.logo | image_url: width: 500 | prepend: "https:" | json }},
    {% endif %}
    "sameAs": [
      {{ settings.social_twitter_link | json }},
      {{ settings.social_facebook_link | json }},
      {{ settings.social_pinterest_link | json }},
      {{ settings.social_instagram_link | json }},
      {{ settings.social_tiktok_link | json }},
      {{ settings.social_tumblr_link | json }},
      {{ settings.social_snapchat_link | json }},
      {{ settings.social_youtube_link | json }},
      {{ settings.social_vimeo_link | json }}
    ],
    "url": {{ request.origin | append: page.url | json }}
  }
</script>

{%- if request.page_type == 'index' -%}
  {% assign potential_action_target = request.origin | append: routes.search_url | append: '?q={search_term_string}' %}
  <script type="application/ld+json">
    {
      "@context": "http://schema.org",
      "@type": "WebSite",
      "name": {{ shop.name | json }},
      "potentialAction": {
        "@type": "SearchAction",
        "target": {{ potential_action_target | json }},
        "query-input": "required name=search_term_string"
      },
      "url": {{ request.origin | append: page.url | json }}
    }
  </script>
{%- endif -%}

{% schema %}
{
  "name": "t:sections.header.name",
  "class": "section-header",
  "max_blocks": 3,
  "settings": [
    {
      "type": "select",
      "id": "logo_position",
      "options": [
        {
          "value": "top-left",
          "label": "t:sections.header.settings.logo_position.options__2.label"
        },
        {
          "value": "top-center",
          "label": "t:sections.header.settings.logo_position.options__3.label"
        },
        {
          "value": "middle-left",
          "label": "t:sections.header.settings.logo_position.options__1.label"
        },
        {
          "value": "middle-center",
          "label": "t:sections.header.settings.logo_position.options__4.label"
        }
      ],
      "default": "middle-left",
      "label": "t:sections.header.settings.logo_position.label",
      "info": "t:sections.header.settings.logo_help.content"
    },
    {
      "type": "select",
      "id": "mobile_logo_position",
      "options": [
        {
          "value": "center",
          "label": "t:sections.header.settings.mobile_logo_position.options__1.label"
        },
        {
          "value": "left",
          "label": "t:sections.header.settings.mobile_logo_position.options__2.label"
        }
      ],
      "default": "center",
      "label": "t:sections.header.settings.mobile_logo_position.label"
    },
    {
      "type": "link_list",
      "id": "menu",
      "default": "main-menu",
      "label": "t:sections.header.settings.menu.label"
    },
    {
      "type": "select",
      "id": "menu_type_desktop",
      "options": [
        {
          "value": "dropdown",
          "label": "t:sections.header.settings.menu_type_desktop.options__1.label"
        },
        {
          "value": "mega",
          "label": "t:sections.header.settings.menu_type_desktop.options__2.label"
        },
        {
          "value": "drawer",
          "label": "t:sections.header.settings.menu_type_desktop.options__3.label"
        }
      ],
      "default": "dropdown",
      "label": "t:sections.header.settings.menu_type_desktop.label"
    },
    {
      "type": "select",
      "id": "sticky_header_type",
      "options": [
        {
          "value": "none",
          "label": "t:sections.header.settings.sticky_header_type.options__1.label"
        },
        {
          "value": "on-scroll-up",
          "label": "t:sections.header.settings.sticky_header_type.options__2.label"
        },
        {
          "value": "always",
          "label": "t:sections.header.settings.sticky_header_type.options__3.label"
        },
        {
          "value": "reduce-logo-size",
          "label": "t:sections.header.settings.sticky_header_type.options__4.label"
        }
      ],
      "default": "on-scroll-up",
      "label": "t:sections.header.settings.sticky_header_type.label"
    },
    {
      "type": "checkbox",
      "id": "show_line_separator",
      "default": true,
      "label": "t:sections.header.settings.show_line_separator.label"
    },
    {
      "type": "header",
      "content": "t:sections.header.settings.header__1.content"
    },
    {
      "type": "color_scheme",
      "id": "color_scheme",
      "label": "t:sections.all.colors.label",
      "default": "scheme-1"
    },
    {
      "type": "color_scheme",
      "id": "menu_color_scheme",
      "label": "t:sections.header.settings.menu_color_scheme.label",
      "default": "scheme-1"
    },
    {
      "type": "header",
      "content": "t:sections.header.settings.header__utilities.content"
    },
    {
      "type": "checkbox",
      "id": "enable_country_selector",
      "default": true,
      "label": "t:sections.header.settings.enable_country_selector.label",
      "info": "t:sections.header.settings.enable_country_selector.info"
    },
    {
      "type": "checkbox",
      "id": "enable_language_selector",
      "default": true,
      "label": "t:sections.header.settings.enable_language_selector.label",
      "info": "t:sections.header.settings.enable_language_selector.info"
    },
    {
      "type": "checkbox",
      "id": "enable_customer_avatar",
      "default": true,
      "label": "t:sections.header.settings.enable_customer_avatar.label",
      "info": "t:sections.header.settings.enable_customer_avatar.info"
    },
    {
      "type": "header",
      "content": "t:sections.all.spacing"
    },
    {
      "type": "range",
      "id": "margin_bottom",
      "min": 0,
      "max": 100,
      "step": 4,
      "unit": "px",
      "label": "t:sections.header.settings.margin_bottom.label",
      "default": 0
    },
    {
      "type": "header",
      "content": "t:sections.all.padding.section_padding_heading"
    },
    {
      "type": "range",
      "id": "padding_top",
      "min": 0,
      "max": 36,
      "step": 4,
      "unit": "px",
      "label": "t:sections.all.padding.padding_top",
      "default": 20
    },
    {
      "type": "range",
      "id": "padding_bottom",
      "min": 0,
      "max": 36,
      "step": 4,
      "unit": "px",
      "label": "t:sections.all.padding.padding_bottom",
      "default": 20
    }
  ],
  "blocks": [
    {
      "type": "@app"
    },
    {
      "type": "custom_dropdown",
      "name": "Custom Dropdown",
      "settings": [
        {
          "type": "text",
          "id": "menu_handle",
          "label": "Menu Handle",
          "info": "Enter the handle of the parent menu item this block belongs to (e.g. shop-by-category)"
        },
        {
          "type": "text",
          "id": "childlink_title",
          "label": "Childlink Title Match",
          "default": "Custom",
          "info": "The submenu item title that triggers this custom dropdown (case-insensitive)"
        },
        {
          "type": "header",
          "content": "Card 1"
        },
        {
          "type": "image_picker",
          "id": "card_1_image",
          "label": "Image"
        },
        {
          "type": "text",
          "id": "card_1_title",
          "label": "Title"
        },
        {
          "type": "url",
          "id": "card_1_url",
          "label": "URL"
        },
        {
          "type": "header",
          "content": "Card 2"
        },
        {
          "type": "image_picker",
          "id": "card_2_image",
          "label": "Image"
        },
        {
          "type": "text",
          "id": "card_2_title",
          "label": "Title"
        },
        {
          "type": "url",
          "id": "card_2_url",
          "label": "URL"
        },
        {
          "type": "header",
          "content": "Card 3"
        },
        {
          "type": "image_picker",
          "id": "card_3_image",
          "label": "Image"
        },
        {
          "type": "text",
          "id": "card_3_title",
          "label": "Title"
        },
        {
          "type": "url",
          "id": "card_3_url",
          "label": "URL"
        },
        {
          "type": "header",
          "content": "Card 4"
        },
        {
          "type": "image_picker",
          "id": "card_4_image",
          "label": "Image"
        },
        {
          "type": "text",
          "id": "card_4_title",
          "label": "Title"
        },
        {
          "type": "url",
          "id": "card_4_url",
          "label": "URL"
        },
        {
          "type": "header",
          "content": "Card 5"
        },
        {
          "type": "image_picker",
          "id": "card_5_image",
          "label": "Image"
        },
        {
          "type": "text",
          "id": "card_5_title",
          "label": "Title"
        },
        {
          "type": "url",
          "id": "card_5_url",
          "label": "URL"
        },
        {
          "type": "header",
          "content": "Card 6"
        },
        {
          "type": "image_picker",
          "id": "card_6_image",
          "label": "Image"
        },
        {
          "type": "text",
          "id": "card_6_title",
          "label": "Title"
        },
        {
          "type": "url",
          "id": "card_6_url",
          "label": "URL"
        }
      ]
    },
    {
      "type": "custom_bestseller",
      "name": "Best Seller Dropdown",
      "settings": [
        {
          "type": "text",
          "id": "menu_handle",
          "label": "Menu Handle",
          "info": "Enter the handle of the parent menu item this block belongs to (e.g. best-sellers)"
        },
        {
          "type": "text",
          "id": "childlink_title",
          "label": "Childlink Title Match",
          "default": "Custom-Best-Seller",
          "info": "The submenu item title that triggers this dropdown (case-insensitive)"
        },
        {
          "type": "header",
          "content": "Card 1"
        },
        {
          "type": "image_picker",
          "id": "card_1_image",
          "label": "Image"
        },
        {
          "type": "text",
          "id": "card_1_title",
          "label": "Title"
        },
        {
          "type": "url",
          "id": "card_1_url",
          "label": "URL"
        },
        {
          "type": "header",
          "content": "Card 2"
        },
        {
          "type": "image_picker",
          "id": "card_2_image",
          "label": "Image"
        },
        {
          "type": "text",
          "id": "card_2_title",
          "label": "Title"
        },
        {
          "type": "url",
          "id": "card_2_url",
          "label": "URL"
        },
        {
          "type": "header",
          "content": "Card 3"
        },
        {
          "type": "image_picker",
          "id": "card_3_image",
          "label": "Image"
        },
        {
          "type": "text",
          "id": "card_3_title",
          "label": "Title"
        },
        {
          "type": "url",
          "id": "card_3_url",
          "label": "URL"
        },
        {
          "type": "header",
          "content": "Card 4"
        },
        {
          "type": "image_picker",
          "id": "card_4_image",
          "label": "Image"
        },
        {
          "type": "text",
          "id": "card_4_title",
          "label": "Title"
        },
        {
          "type": "url",
          "id": "card_4_url",
          "label": "URL"
        },
        {
          "type": "header",
          "content": "Card 5"
        },
        {
          "type": "image_picker",
          "id": "card_5_image",
          "label": "Image"
        },
        {
          "type": "text",
          "id": "card_5_title",
          "label": "Title"
        },
        {
          "type": "url",
          "id": "card_5_url",
          "label": "URL"
        },
        {
          "type": "header",
          "content": "Card 6"
        },
        {
          "type": "image_picker",
          "id": "card_6_image",
          "label": "Image"
        },
        {
          "type": "text",
          "id": "card_6_title",
          "label": "Title"
        },
        {
          "type": "url",
          "id": "card_6_url",
          "label": "URL"
        }
      ]
    },
    {
      "type": "custom_product_dropdown",
      "name": "Custom Product Dropdown",
      "settings": [
        {
          "type": "text",
          "id": "menu_handle",
          "label": "Menu Handle",
          "info": "Enter the handle of the parent menu item this block belongs to (e.g. shop-by-category)"
        },
        {
          "type": "text",
          "id": "childlink_title",
          "label": "Childlink Title Match",
          "default": "Custom-Product",
          "info": "The submenu item title that triggers this custom product dropdown (case-insensitive)"
        },
        {
          "type": "text",
          "id": "slider_heading",
          "label": "Slider Heading"
        },
        {
          "type": "checkbox",
          "id": "override_title",
          "label": "Override product title",
          "default": false
        },
        {
          "type": "text",
          "id": "custom_title",
          "label": "Custom title (fallback)",
          "info": "Used when \"Override product title\" is enabled and a per-slide title is not set"
        },
        {
          "type": "checkbox",
          "id": "override_description",
          "label": "Override product description",
          "default": false
        },
        {
          "type": "textarea",
          "id": "custom_description",
          "label": "Custom description",
          "info": "When override is on, this text is shown for every slide (per-slide descriptions are ignored)."
        },
        {
          "type": "product",
          "id": "product_1",
          "label": "Product 1"
        },
        {
          "type": "product",
          "id": "product_2",
          "label": "Product 2"
        },
        {
          "type": "product",
          "id": "product_3",
          "label": "Product 3"
        },
        {
          "type": "product",
          "id": "product_4",
          "label": "Product 4"
        },
        {
          "type": "product",
          "id": "product_5",
          "label": "Product 5"
        },
        {
          "type": "product",
          "id": "product_6",
          "label": "Product 6"
        },
        {
          "type": "product",
          "id": "product_7",
          "label": "Product 7"
        },
        {
          "type": "product",
          "id": "product_8",
          "label": "Product 8"
        },
        {
          "type": "product",
          "id": "product_9",
          "label": "Product 9"
        },
        {
          "type": "product",
          "id": "product_10",
          "label": "Product 10"
        },
        {
          "type": "header",
          "content": "Per-product titles"
        },
        {
          "type": "paragraph",
          "content": "Optional title for each slide. If set, it is used for that slide instead of the product name. Otherwise the global override above applies, then the product title."
        },
        {
          "type": "text",
          "id": "product_1_title",
          "label": "Product 1 title"
        },
        {
          "type": "text",
          "id": "product_2_title",
          "label": "Product 2 title"
        },
        {
          "type": "text",
          "id": "product_3_title",
          "label": "Product 3 title"
        },
        {
          "type": "text",
          "id": "product_4_title",
          "label": "Product 4 title"
        },
        {
          "type": "text",
          "id": "product_5_title",
          "label": "Product 5 title"
        },
        {
          "type": "text",
          "id": "product_6_title",
          "label": "Product 6 title"
        },
        {
          "type": "text",
          "id": "product_7_title",
          "label": "Product 7 title"
        },
        {
          "type": "text",
          "id": "product_8_title",
          "label": "Product 8 title"
        },
        {
          "type": "text",
          "id": "product_9_title",
          "label": "Product 9 title"
        },
        {
          "type": "text",
          "id": "product_10_title",
          "label": "Product 10 title"
        },
        {
          "type": "header",
          "content": "Per-product descriptions"
        },
        {
          "type": "paragraph",
          "content": "If global override is on and custom description is filled, that single text is used for every slide and these per-slide fields are ignored. Otherwise, per-slide text is used when set, then the product description."
        },
        {
          "type": "textarea",
          "id": "product_1_description",
          "label": "Product 1 description"
        },
        {
          "type": "textarea",
          "id": "product_2_description",
          "label": "Product 2 description"
        },
        {
          "type": "textarea",
          "id": "product_3_description",
          "label": "Product 3 description"
        },
        {
          "type": "textarea",
          "id": "product_4_description",
          "label": "Product 4 description"
        },
        {
          "type": "textarea",
          "id": "product_5_description",
          "label": "Product 5 description"
        },
        {
          "type": "textarea",
          "id": "product_6_description",
          "label": "Product 6 description"
        },
        {
          "type": "textarea",
          "id": "product_7_description",
          "label": "Product 7 description"
        },
        {
          "type": "textarea",
          "id": "product_8_description",
          "label": "Product 8 description"
        },
        {
          "type": "textarea",
          "id": "product_9_description",
          "label": "Product 9 description"
        },
        {
          "type": "textarea",
          "id": "product_10_description",
          "label": "Product 10 description"
        }
      ]
    }
  ]
}
{% endschema %}
