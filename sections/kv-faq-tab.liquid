{% comment %}
  FAQ: add "Tab" blocks for nav (+ optional section titles) and "FAQ" blocks for Q&A (match Tab section link).
  Sticky tabs, scrollspy, accordions (one open per group).
{% endcomment %}

<section
  class="kv-section kv-faq-page"
  id="kv-faq-page-{{ section.id }}"
  data-kv-faq-root
  data-kv-header-offset="{{ section.settings.header_offset | default: 0 }}"
  style="--kv-faq-sticky-top: {{ section.settings.header_offset | default: 0 }}px; --kv-faq-scroll-margin: calc({{ section.settings.header_offset | default: 0 }}px + 76px);"
>
  <nav class="kv-faq-tabs" aria-label="FAQ categories" data-kv-faq-tabs data-kv-faq-tabs-nav>
    <div class="kv-faq-tabs__track" data-kv-faq-tabs-track>
      <button type="button" class="kv-faq-tab kv-faq-tab--active" data-kv-faq-tab data-target="all">All</button>
      {% assign faq_tab_count = 0 %}
      {% for block in section.blocks %}
        {% if block.type == 'faq_tab' %}
          {% assign faq_tab_count = faq_tab_count | plus: 1 %}
        {% endif %}
      {% endfor %}
      {% if faq_tab_count == 0 %}
        {% if request.design_mode %}
          <p class="kv-faq-editor-hint">Add <strong>Tab</strong> blocks for navigation (All is fixed first), then <strong>FAQ</strong> blocks for questions (same section key on each).</p>
        {% endif %}
      {% else %}
        {% for block in section.blocks %}
          {% if block.type == 'faq_tab' %}
            {% assign tab_slug = block.settings.section_key | strip | handleize %}
            {% if tab_slug == blank %}
              {% continue %}
            {% endif %}
            {% assign tab_title = block.settings.label | strip %}
            {% if tab_title == blank %}
              {% assign tab_title = block.settings.section_key | strip %}
            {% endif %}
            <button
              type="button"
              class="kv-faq-tab"
              data-kv-faq-tab
              data-target="{{ tab_slug | escape }}"
              {{ block.shopify_attributes }}
            >
              {{- tab_title | escape -}}
            </button>
          {% endif %}
        {% endfor %}
      {% endif %}
    </div>
  </nav>

  <div class="kv-faq-content" data-kv-faq-content>
    <div class="kv-faq-anchor" id="kv-faq-top-{{ section.id }}" data-kv-faq-section="all" aria-hidden="true"></div>

    {% assign rendered_sec = '' %}
    {% for block in section.blocks %}
      {% if block.type == 'faq_tab' %}
        {% assign sec_handle = block.settings.section_key | strip | handleize %}
        {% if sec_handle == blank %}
          {% continue %}
        {% endif %}
        {% assign sec_mark = sec_handle | prepend: '|' | append: '|' %}
        {% if rendered_sec contains sec_mark %}
          {% continue %}
        {% endif %}
        {% assign rendered_sec = rendered_sec | append: sec_mark %}
        <section
          class="kv-faq-block"
          id="kv-faq-{{ sec_handle }}-{{ section.id }}"
          data-kv-faq-section="{{ sec_handle | escape }}"
          aria-labelledby="kv-faq-heading-{{ sec_handle }}-{{ section.id }}"
        >
          <h2 class="kv-font-38" id="kv-faq-heading-{{ sec_handle }}-{{ section.id }}">
            {% if block.settings.heading_inline != blank %}
              {{ block.settings.heading_inline }}
            {% else %}
              {% case sec_handle %}
                {% when 'about' %}
                  About <em>Bollit</em>
                {% when 'product' %}
                  Product <em>design & detail</em>
                {% when 'tracking' %}
                  Tracking <em>& use</em>
                {% when 'shipping' %}
                  Shipping
                {% when 'returns' %}
                  Returns <em>& warranty</em>
                {% else %}
                  {{ sec_handle | replace: '-', ' ' | capitalize }}
              {% endcase %}
            {% endif %}
          </h2>
          <div class="kv-faq-accordion" data-kv-faq-accordion>
            {% assign first_faq = true %}
            {% for b in section.blocks %}
              {% if b.type == 'faq_item' %}
                {% assign item_handle = b.settings.section_key | strip | handleize %}
                {% if item_handle == sec_handle %}
                  <div
                    class="kv-faq-item{% if first_faq %} kv-faq-item--open{% endif %}"
                    {{ b.shopify_attributes }}
                  >
                    <button
                      type="button"
                      class="kv-faq-item__trigger"
                      aria-expanded="{% if first_faq %}true{% else %}false{% endif %}"
                    >
                      <span class="kv-faq-item__question">{{ b.settings.question | escape }}</span>
                      <span class="kv-faq-item__icon" aria-hidden="true">
                        <svg xmlns="http://www.w3.org/2000/svg" width="16" height="16" viewBox="0 0 16 16" fill="none">
                        <path d="M13 6L8 11L3 6" stroke="#808080" stroke-linecap="round" stroke-linejoin="round"/>
                        </svg>
                      </span>
                    </button>
                    <div class="kv-faq-item__panel">
                      <div class="kv-faq-item__panel-inner">
                        <div class="kv-faq-item__answer rte">{{ b.settings.answer }}</div>
                      </div>
                    </div>
                  </div>
                  {% assign first_faq = false %}
                {% endif %}
              {% endif %}
            {% endfor %}
          </div>
        </section>
      {% endif %}
    {% endfor %}

    {% for block in section.blocks %}
      {% if block.type == 'faq_item' %}
        {% assign sec_handle = block.settings.section_key | strip | handleize %}
        {% if sec_handle == blank %}
          {% continue %}
        {% endif %}
        {% assign sec_mark = sec_handle | prepend: '|' | append: '|' %}
        {% if rendered_sec contains sec_mark %}
          {% continue %}
        {% endif %}
        {% assign rendered_sec = rendered_sec | append: sec_mark %}
        <section
          class="kv-faq-block"
          id="kv-faq-{{ sec_handle }}-{{ section.id }}"
          data-kv-faq-section="{{ sec_handle | escape }}"
          aria-labelledby="kv-faq-heading-{{ sec_handle }}-{{ section.id }}"
        >
          <h2 class="kv-faq-block__title" id="kv-faq-heading-{{ sec_handle }}-{{ section.id }}">
            {% case sec_handle %}
              {% when 'about' %}
                About <em>Bollit</em>
              {% when 'product' %}
                Product <em>design & detail</em>
              {% when 'tracking' %}
                Tracking <em>& use</em>
              {% when 'shipping' %}
                Shipping
              {% when 'returns' %}
                Returns <em>& warranty</em>
              {% else %}
                {{ sec_handle | replace: '-', ' ' | capitalize }}
            {% endcase %}
          </h2>
          <div class="kv-faq-accordion" data-kv-faq-accordion>
            {% assign first_faq = true %}
            {% for b in section.blocks %}
              {% if b.type == 'faq_item' %}
                {% assign item_handle = b.settings.section_key | strip | handleize %}
                {% if item_handle == sec_handle %}
                  <div
                    class="kv-faq-item{% if first_faq %} kv-faq-item--open{% endif %}"
                    {{ b.shopify_attributes }}
                  >
                    <button
                      type="button"
                      class="kv-faq-item__trigger"
                      aria-expanded="{% if first_faq %}true{% else %}false{% endif %}"
                    >
                      <span class="kv-faq-item__question">{{ b.settings.question | escape }}</span>
                      <span class="kv-faq-item__icon" aria-hidden="true">
                        <svg xmlns="http://www.w3.org/2000/svg" width="16" height="16" viewBox="0 0 16 16" fill="none">
                        <path d="M13 6L8 11L3 6" stroke="#808080" stroke-linecap="round" stroke-linejoin="round"/>
                        </svg>
                      </span>
                    </button>
                    <div class="kv-faq-item__panel">
                      <div class="kv-faq-item__panel-inner">
                        <div class="kv-faq-item__answer rte">{{ b.settings.answer }}</div>
                      </div>
                    </div>
                  </div>
                  {% assign first_faq = false %}
                {% endif %}
              {% endif %}
            {% endfor %}
          </div>
        </section>
      {% endif %}
    {% endfor %}
  </div>
</section>

<style>
  .kv-faq-editor-hint {
    margin: 0;
    padding: 0.75rem 1rem;
    font-size: 0.9rem;
    color: var(--kv-faq-muted);
    line-height: 1.45;
  }

  .kv-faq-page {
    --kv-faq-blue: #003665;
    --kv-faq-blue-soft: #F0F8FF;
    --kv-faq-text: #1F1F1F;
    --kv-faq-muted: #808080;
    --kv-faq-radius: 20px;
    --kv-faq-sticky-top: 0px;
    max-width: 800px;
    margin: 0 auto;
    color: var(--kv-faq-text);
  }

  .kv-faq-tabs {
    position: relative;
    z-index: 10;
    background: #fff;
    padding: 0;
    transition: border-color 0.25s ease, box-shadow 0.25s ease;
  }

  .kv-faq-tabs.kv-faq-tabs--pinned {
    position: sticky;
    top: var(--kv-faq-sticky-top);
    border-bottom-color: #eee;
  }

  .kv-faq-tabs__track {
    display: flex;
    flex-wrap: nowrap;
    gap: 3px;
    overflow-x: auto;
    overflow-y: hidden;
    -webkit-overflow-scrolling: touch;
    scrollbar-width: thin;
    scroll-behavior: smooth;
    padding-bottom: 0px;
  }

  .kv-faq-tabs__track::-webkit-scrollbar {
    height: 4px;
  }

  .kv-faq-tab {
    flex: 0 0 auto;
    border: none;
    background: transparent;
    color: var(--kv-faq-muted);
    font-size: 18px;
    font-weight: 500;
    padding: 12px;
    border-radius: 8px 8px 0 0;
    cursor: pointer;
    white-space: nowrap;
    transition: color 0.2s ease, background 0.2s ease, box-shadow 0.2s ease;
    border-bottom: 1px solid transparent;
  }

  .kv-faq-tab:hover {
    color: var(--kv-faq-text);
  }

  .kv-faq-tab--active {
    color: var(--kv-faq-blue);
    background: var(--kv-faq-blue-soft);
    border-bottom: 1px solid #003665;
  }

  .kv-faq-anchor {
    height: 1px;
    margin: 0;
    padding: 0;
    pointer-events: none;
  }

  .kv-faq-content {
    padding-top:0;
  }

  .kv-faq-block {
    scroll-margin-top: var(--kv-faq-scroll-margin, calc(var(--kv-faq-sticky-top) + 76px));
    margin-bottom: 3.5rem;
  }

  .kv-faq-block__title {
    text-align: center;
    font-size: clamp(1.5rem, 4vw, 2rem);
    font-weight: 600;
    margin: 0 0 1.5rem;
    line-height: 1.2;
  }

  .kv-faq-block__title em {
    font-family: Georgia, 'Times New Roman', serif;
    font-style: italic;
    font-weight: 500;
    color: var(--kv-faq-blue);
  }

  .kv-faq-accordion {
    background: var(--kv-faq-blue-soft);
    border-radius: var(--kv-faq-radius);
    padding: 8px 24px;
  }

  .kv-faq-item {
    border-bottom: 1px solid rgba(30, 107, 184, 0.12);
  }

  .kv-faq-item:last-child {
    border-bottom: none;
  }

  .kv-faq-item__trigger {
    width: 100%;
    display: flex;
    align-items: center;
    justify-content: space-between;
    gap: 1rem;
    text-align: left;
    padding: 16px 0px;
    border: none;
    background: none;
    font: inherit;
    color: inherit;
    cursor: pointer;
  }
.kv-faq-item__question {
    font-weight: 500;
    font-size: 20px;
    color: #1F1F1F;
    line-height: normal;
}

  .kv-faq-item__icon {
    transition: transform 0.25s ease;
  }

  .kv-faq-item--open .kv-faq-item__icon {
    transform: rotate(-180deg);
  }

  .kv-faq-item__panel {
    display: grid;
    grid-template-rows: 0fr;
    transition: grid-template-rows 0.4s cubic-bezier(0.4, 0, 0.2, 1);
  }

  .kv-faq-item--open .kv-faq-item__panel {
    grid-template-rows: 1fr;
  }

  .kv-faq-item__panel-inner {
    overflow: hidden;
    min-height: 0;
  }

  .kv-faq-item__panel .kv-faq-item__answer {
    padding: 0px 0px 12px 0px;
    opacity: 0;
    transform: translateY(-6px);
    transition: opacity 0.35s ease 0.05s, transform 0.35s cubic-bezier(0.4, 0, 0.2, 1) 0.05s;
  }

  .kv-faq-item--open .kv-faq-item__panel .kv-faq-item__answer {
    opacity: 1;
    transform: translateY(0);
  }
  .kv-faq-block .kv-font-38 {
    margin: 42px 0px 20px;
    line-height: normal;
}

  @media (prefers-reduced-motion: reduce) {
    .kv-faq-item__panel {
      transition-duration: 0.01ms;
    }
    .kv-faq-item__panel .kv-faq-item__answer {
      transition-duration: 0.01ms;
      transition-delay: 0ms;
    }
  }

  .kv-faq-item__answer {
    font-size: 0.95rem;
    line-height: 1.55;
    color: #333;
  }

  .kv-faq-item__answer p {
    color: rgba(31, 31, 31, 0.90);
    font-size: 18px;
    font-style: normal;
    font-weight: 400;
    line-height: normal;
     margin:0px;
  }

  .kv-faq-item__answer p:last-child {
    margin-bottom: 0;
  }
</style>

<script>
  (function () {
    var root = document.querySelector('[data-kv-faq-root]');
    if (!root) return;
    var headerOffset = parseInt(root.getAttribute('data-kv-header-offset'), 10) || 0;

    var tabsNav = root.querySelector('[data-kv-faq-tabs-nav]');
    var track = root.querySelector('[data-kv-faq-tabs-track]');
    var tabButtons = root.querySelectorAll('[data-kv-faq-tab]');

    function scrollTabIntoView(btn) {
      if (!track || !btn) return;
      var tr = track.getBoundingClientRect();
      var br = btn.getBoundingClientRect();
      var pad = 16;
      var left = track.scrollLeft;
      if (br.left < tr.left + pad) {
        left += br.left - tr.left - pad;
      } else if (br.right > tr.right - pad) {
        left += br.right - tr.right + pad;
      } else return;
      track.scrollTo({ left: left, behavior: 'smooth' });
    }

    function setActiveTab(key) {
      tabButtons.forEach(function (b) {
        var on = b.getAttribute('data-target') === key;
        b.classList.toggle('kv-faq-tab--active', on);
        if (on) scrollTabIntoView(b);
      });
    }

    var scrollSpyLock = false;
    function scrollToSection(key) {
      var id;
      if (key === 'all') {
        id = root.querySelector('[data-kv-faq-section="all"]');
      } else {
        var el = root.querySelector('[data-kv-faq-section="' + key + '"]');
        id = el;
      }
      if (!id) return;
      scrollSpyLock = true;
      pinAllowed = true;
      setActiveTab(key);
      id.scrollIntoView({ behavior: 'smooth', block: 'start' });
      setTimeout(function () {
        scrollSpyLock = false;
      }, 800);
    }

    tabButtons.forEach(function (btn) {
      btn.addEventListener('click', function () {
        var key = btn.getAttribute('data-target');
        if (key) scrollToSection(key);
      });
    });

    var sectionEls = Array.prototype.slice
      .call(root.querySelectorAll('[data-kv-faq-section]'))
      .filter(function (el) {
        return el.getAttribute('data-kv-faq-section') !== 'all';
      });

    var stickyOffset = headerOffset + 76;
    var scrollSpyTicking = false;
    var lastScrollY = window.scrollY || document.documentElement.scrollTop || 0;
    var pinAllowed = true;
    var upEps = 10;
    var downEps = 2;

    function applyTabsPinGeometry() {
      if (!tabsNav || !pinAllowed) {
        if (tabsNav) tabsNav.classList.remove('kv-faq-tabs--pinned');
        return;
      }
      var rootTop = root.getBoundingClientRect().top;
      var pin = rootTop <= headerOffset + 8;
      tabsNav.classList.toggle('kv-faq-tabs--pinned', pin);
    }

    function updateTabsPinFromScroll(y) {
      if (!tabsNav) return;
      var delta = y - lastScrollY;
      lastScrollY = y;
      if (delta < -upEps) {
        pinAllowed = false;
        tabsNav.classList.remove('kv-faq-tabs--pinned');
        return;
      }
      if (delta > downEps) {
        pinAllowed = true;
      }
      applyTabsPinGeometry();
    }

    function activeSectionFromScroll() {
      if (!sectionEls.length) return 'all';
      var first = sectionEls[0];
      if (first.getBoundingClientRect().top > stickyOffset + 32) return 'all';
      var active = sectionEls[0].getAttribute('data-kv-faq-section');
      for (var i = 0; i < sectionEls.length; i++) {
        var el = sectionEls[i];
        var top = el.getBoundingClientRect().top;
        if (top <= stickyOffset + 24) {
          active = el.getAttribute('data-kv-faq-section');
        }
      }
      return active;
    }

    function onScrollSpy() {
      if (scrollSpyLock) return;
      var next = activeSectionFromScroll();
      setActiveTab(next);
    }

    function onScroll() {
      if (scrollSpyTicking) return;
      scrollSpyTicking = true;
      requestAnimationFrame(function () {
        scrollSpyTicking = false;
        var y = window.scrollY || document.documentElement.scrollTop || 0;
        updateTabsPinFromScroll(y);
        if (!scrollSpyLock) onScrollSpy();
      });
    }

    window.addEventListener('scroll', onScroll, { passive: true });
    window.addEventListener('resize', function () {
      lastScrollY = window.scrollY || document.documentElement.scrollTop || 0;
      pinAllowed = true;
      if (scrollSpyTicking) return;
      scrollSpyTicking = true;
      requestAnimationFrame(function () {
        scrollSpyTicking = false;
        applyTabsPinGeometry();
        if (!scrollSpyLock) onScrollSpy();
      });
    }, { passive: true });
    applyTabsPinGeometry();
    onScrollSpy();

    root.querySelectorAll('[data-kv-faq-accordion]').forEach(function (acc) {
      acc.addEventListener('click', function (e) {
        var trigger = e.target.closest('.kv-faq-item__trigger');
        if (!trigger || !acc.contains(trigger)) return;
        var item = trigger.closest('.kv-faq-item');
        if (!item) return;
        var willOpen = !item.classList.contains('kv-faq-item--open');

        acc.querySelectorAll('.kv-faq-item').forEach(function (other) {
          var oTrig = other.querySelector('.kv-faq-item__trigger');
          other.classList.remove('kv-faq-item--open');
          if (oTrig) oTrig.setAttribute('aria-expanded', 'false');
        });

        if (willOpen) {
          item.classList.add('kv-faq-item--open');
          trigger.setAttribute('aria-expanded', 'true');
        }
      });
    });
  })();
</script>

{% schema %}
{
  "name": "KV FAQ tabs",
  "tag": "section",
  "class": "kv-faq-tab-section",
  "settings": [
    {
      "type": "range",
      "id": "header_offset",
      "min": 0,
      "max": 200,
      "step": 2,
      "unit": "px",
      "label": "Offset below fixed header",
      "info": "Set to your theme header height so sticky tabs sit below it and scroll-to-section lines up.",
      "default": 0
    }
  ],
  "blocks": [
    {
      "type": "faq_tab",
      "name": "Tab",
      "limit": 20,
      "settings": [
        {
          "type": "text",
          "id": "section_key",
          "label": "Section key",
          "info": "Use your own text (e.g. about, care-guide, returns). Must match FAQ blocks."
        },
        {
          "type": "inline_richtext",
          "id": "heading_inline",
          "label": "Section heading",
          "info": "Single heading field (supports inline formatting)."
        },
        {
          "type": "text",
          "id": "label",
          "label": "Tab label",
          "info": "Optional. Blank uses a default label."
        }
      ]
    },
    {
      "type": "faq_item",
      "name": "FAQ",
      "limit": 80,
      "settings": [
        {
          "type": "text",
          "id": "section_key",
          "label": "Section key",
          "info": "Must match a Tab section key."
        },
        { "type": "text", "id": "question", "label": "Question", "default": "Your question?" },
        {
          "type": "richtext",
          "id": "answer",
          "label": "Answer",
          "default": "<p>Your answer.</p>"
        }
      ]
    }
  ],
  "presets": [
    {
      "name": "KV FAQ tabs",
      "blocks": [
        { "type": "faq_tab", "settings": { "section_key": "about", "heading_inline": "About <em>Bollit</em>", "label": "About Bollit" } },
        { "type": "faq_tab", "settings": { "section_key": "product", "heading_inline": "Product <em>design & detail</em>", "label": "Product details & design" } },
        { "type": "faq_tab", "settings": { "section_key": "tracking", "heading_inline": "Tracking <em>& use</em>", "label": "Tracking & Use" } },
        { "type": "faq_tab", "settings": { "section_key": "shipping", "heading_inline": "Shipping", "label": "Shipping" } },
        { "type": "faq_tab", "settings": { "section_key": "returns", "heading_inline": "Returns <em>& warranty</em>", "label": "Returns & Warranty" } },
        {
          "type": "faq_item",
          "settings": {
            "section_key": "about",
            "question": "What is Bollit?",
            "answer": "<p>Bollit creates modern cleaning tools designed for real life—simple to use, easy to store, and built to last.</p>"
          }
        },
        {
          "type": "faq_item",
          "settings": {
            "section_key": "about",
            "question": "Where are Bollit products made?",
            "answer": "<p>We partner with trusted manufacturers and share country of origin on each product page.</p>"
          }
        },
        {
          "type": "faq_item",
          "settings": {
            "section_key": "about",
            "question": "What makes Bollit different?",
            "answer": "<p>We focus on design, durability, and a calm, uncluttered experience in every product.</p>"
          }
        },
        {
          "type": "faq_item",
          "settings": {
            "section_key": "about",
            "question": "Where does Bollit ship to?",
            "answer": "<p>We ship to many countries; availability is shown at checkout.</p>"
          }
        },
        {
          "type": "faq_item",
          "settings": {
            "section_key": "product",
            "question": "What materials are used in Bollit products?",
            "answer": "<p>Materials vary by product; each listing describes plastics, metals, fibers, and care instructions.</p>"
          }
        },
        {
          "type": "faq_item",
          "settings": {
            "section_key": "product",
            "question": "Are Bollit products easy to use?",
            "answer": "<p>Yes—ergonomics and clear instructions are part of every design.</p>"
          }
        },
        {
          "type": "faq_item",
          "settings": {
            "section_key": "product",
            "question": "How durable are Bollit cleaning tools?",
            "answer": "<p>We test for regular home use; warranty terms apply where stated.</p>"
          }
        },
        {
          "type": "faq_item",
          "settings": {
            "section_key": "product",
            "question": "Do Bollit products require assembly?",
            "answer": "<p>Most items are ready to use; any assembly steps are noted on the product page.</p>"
          }
        },
        {
          "type": "faq_item",
          "settings": {
            "section_key": "tracking",
            "question": "How can I track my order?",
            "answer": "<p>Use the tracking link in your shipping confirmation email.</p>"
          }
        },
        {
          "type": "faq_item",
          "settings": {
            "section_key": "shipping",
            "question": "How long does shipping take?",
            "answer": "<p>Delivery times depend on your location and method; estimates appear at checkout.</p>"
          }
        },
        {
          "type": "faq_item",
          "settings": {
            "section_key": "returns",
            "question": "What is your return policy?",
            "answer": "<p>Unused items in original packaging may be returned within the period stated on our returns page.</p>"
          }
        }
      ]
    }
  ]
}
{% endschema %}
